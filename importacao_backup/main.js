//como importa funções de outro arq (funcoesaux)?

const funcoes = require('./funcoesaux');

// mas pra importar precisamos EXPORTAR lá tb!!

console.log(funcoes);
console.log(funcoes.gets());

// qnd a gnt tem umobjeto, a gnt pode DESTRUIR ele
// (object destructuring)

const pessoa {
    nome: lia;
}